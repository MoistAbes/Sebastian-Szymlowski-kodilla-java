package com.kodilla.good.patterns.challanges;

public class ProductOrderRepository implements OrderRepository {
    @Override
    public boolean createOrder(User user, double price) {
        return true;
    }
}
